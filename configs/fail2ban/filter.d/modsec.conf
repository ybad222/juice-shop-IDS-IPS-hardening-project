# /etc/fail2ban/filter.d/modsec.conf
#
# Matches ModSecurity's own "Access denied with code 403" audit-log line and
# extracts the source IP so Fail2ban can act on it.

[Definition]
failregex = .*? \[client <HOST>\] ModSecurity: Access denied with code 403\s
ignoreregex =
